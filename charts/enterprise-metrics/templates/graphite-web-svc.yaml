{{- if eq .Values.graphite.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: graphite-web
  labels:
    app: graphite-web
    chart: {{ template "enterprise-metrics.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    {{- with .Values.graphite_web.service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- toYaml .Values.graphite_web.service.annotations | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.graphite_web.service.http_listen_port }}
      protocol: TCP
      name: graphite-http
      targetPort: 80
  selector:
    app: graphite-web
    release: {{ .Release.Name }}
{{- end -}}
